<% content_for :title, "Buy Indicia — #{@letter.public_id}" %>

<div class="toolbar" style="border-bottom: none; margin-bottom: 0;">
  <div style="display:flex;align-items:center;gap:0.5rem">
    <a href="<%= letter_path(@letter) %>" style="text-decoration: none; color: GrayText;">← <%= @letter.public_id %></a>
    <strong style="font-size: 1.15em;">Buy Indicia</strong>
  </div>
</div>

<div class="show-layout">
  <div class="show-main">
    <section style="margin-bottom: 1rem;">
      <strong>Letter Details</strong>
      <hr>
      <div class="detail-grid" style="margin-top: 0.5rem;">
        <span class="detail-label">Recipient</span>
        <span><%= @letter.address.name_line %></span>
        <span class="detail-label">Destination</span>
        <span><%= @letter.address.city %>, <%= @letter.address.state %></span>
        <span class="detail-label">Dimensions</span>
        <span><%= @letter.height %> × <%= @letter.width %> in, <%= @letter.weight %> oz</span>
      </div>
      <% if @letter.non_machinable? %>
        <hr style="margin-top:0.5rem">
        <div style="margin-top: 0.5rem;">
          <span class="badge badge-warning">⚠ Non-machinable surcharge applies</span>
        </div>
      <% end %>
    </section>

    <section style="margin-bottom: 1rem;">
      <strong>Purchase Postage</strong>
      <hr>
      <div style="margin-top: 0.5rem;">
        <%= form_with(url: buy_indicia_letter_path(@letter), method: :post) do |form| %>
          <div style="margin-bottom: 0.5rem;">
            <label style="display: block; color: GrayText; margin-bottom: 0.25rem;">USPS Payment Account</label>
            <select name="usps_payment_account_id" required style="width: 100%;">
              <option value="">Select account…</option>
              <% USPS::PaymentAccount.all.each do |account| %>
                <option value="<%= account.id %>" <%= "selected" if account.id == USPS::PaymentAccount.first&.id %>><%= account.name %></option>
              <% end %>
            </select>
          </div>

          <% if current_user.hcb_payment_accounts.any? %>
            <div style="margin-bottom: 0.5rem;">
              <label style="display: block; color: GrayText; margin-bottom: 0.25rem;">HCB Organization</label>
              <select name="hcb_payment_account_id" required style="width: 100%;">
                <option value="">Select organization…</option>
                <% current_user.hcb_payment_accounts.each do |account| %>
                  <option value="<%= account.id %>"><%= account.organization_name %></option>
                <% end %>
              </select>
            </div>
          <% else %>
            <div style="margin-bottom: 0.5rem; color: GrayText;">
              <%= link_to "Connect your HCB account", new_hcb_oauth_connection_path %> to pay for postage.
            </div>
          <% end %>

          <div style="display:flex;align-items:center;gap:0.5rem;margin-top:1rem;">
            <span class="spacer"></span>
            <a href="<%= letter_path(@letter) %>" style="color: GrayText;">Cancel</a>
            <button type="submit" class="btn-success" <%= "disabled" if current_user.hcb_payment_accounts.empty? %>>✓ Buy Indicia</button>
          </div>
        <% end %>
      </div>
    </section>
  </div>

  <div class="show-sidebar">
    <section>
      <strong>Info</strong>
      <hr>
      <div style="margin-top: 0.5rem; color: GrayText;">
        Purchasing indicia buys USPS postage for this letter. The cost is charged to the selected HCB organization.
      </div>
    </section>
  </div>
</div>
