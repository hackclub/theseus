<% content_for :title, "Edit Mailer ID" %>

<div class="page-toolbar" style="border-bottom: none; margin-bottom: 0;">
  <row gap-="1" align-="center">
    <a href="<%= usps_mailer_id_path(@usps_mailer_id) %>" style="text-decoration: none; color: var(--foreground2);">← <%= @usps_mailer_id.name %></a>
    <strong style="font-size: 1.15em;">Edit Mailer ID</strong>
  </row>
</div>

<%= render "form", usps_mailer_id: @usps_mailer_id %>
