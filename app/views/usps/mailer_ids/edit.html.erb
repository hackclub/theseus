<% content_for :title, "Edit Mailer ID" %>

<div class="toolbar" style="border-bottom: none; margin-bottom: 0;">
  <div style="display:flex;align-items:center;gap:0.5rem">
    <a href="<%= usps_mailer_id_path(@usps_mailer_id) %>" class="text-muted" style="text-decoration: none;">← <%= @usps_mailer_id.name %></a>
    <strong style="font-size: 1.15em;">Edit Mailer ID</strong>
  </div>
</div>

<%= render "form", usps_mailer_id: @usps_mailer_id %>
