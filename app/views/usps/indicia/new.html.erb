<% content_for :title, "New Indicium" %>

<div class="toolbar mb-0 no-border-bottom">
  <div class="flex-row">
    <a href="<%= usps_indicia_path %>" class="text-muted no-underline">← Indicia</a>
    <strong class="text-title">New Indicium</strong>
  </div>
</div>

<%= render "form", usps_indicium: @usps_indicium %>
