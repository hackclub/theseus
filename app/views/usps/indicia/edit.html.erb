<% content_for :title, "Edit Indicium" %>

<div class="toolbar mb-0 no-border-bottom">
  <div class="flex-row">
    <a href="<%= usps_indicium_path(@usps_indicium) %>" class="text-muted no-underline">← <%= @usps_indicium.public_id %></a>
    <strong class="text-title">Edit Indicium</strong>
  </div>
</div>

<%= render "form", usps_indicium: @usps_indicium %>
