<% content_for :title, "Edit Indicium" %>

<div class="toolbar mb-0" style="border-bottom: none">
  <div class="flex-row">
    <a href="<%= usps_indicium_path(@usps_indicium) %>" class="text-muted" style="text-decoration: none;">← <%= @usps_indicium.public_id %></a>
    <strong style="font-size: 1.15em;">Edit Indicium</strong>
  </div>
</div>

<%= render "form", usps_indicium: @usps_indicium %>
