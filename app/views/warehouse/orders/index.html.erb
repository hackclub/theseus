<% content_for :title, "Orders" %>

<h1>Orders</h1>

<div id="warehouse_orders">
  <% @warehouse_orders.order(created_at: :desc).each do |warehouse_order| %>
    <%= render warehouse_order %>
  <% end %>
</div>

<%= link_to "New order", new_warehouse_order_path %>
