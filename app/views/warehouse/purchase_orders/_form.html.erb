<%= form_with(model: purchase_order, url: purchase_order.persisted? ? warehouse_purchase_order_path(purchase_order) : warehouse_purchase_orders_path) do |form| %>
  <% if purchase_order.errors.any? %>
    <div box-="square" class="tui-banner tui-banner-error" style="margin-bottom: 1lh;">
      <strong><%= pluralize(purchase_order.errors.count, "error") %> prevented saving:</strong>
      <ul class="error-list">
        <% purchase_order.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div box-="round" style="margin-bottom: 1lh;">
    <strong>Supplier Information</strong>
    <div is-="separator"></div>
    <div style="margin-top: 0.5lh;">
      <div style="margin-bottom: 1lh;">
        <label style="display: block; color: var(--foreground2); margin-bottom: 0.25lh;">Supplier name <span style="color: var(--red);">*</span></label>
        <input type="text" name="warehouse_purchase_order[supplier_name]" value="<%= purchase_order.supplier_name %>" placeholder="Enter supplier name" required style="width: 100%;">
      </div>
      <div style="margin-bottom: 1lh;">
        <label style="display: block; color: var(--foreground2); margin-bottom: 0.25lh;">Supplier ID (optional)</label>
        <input type="text" name="warehouse_purchase_order[supplier_id]" value="<%= purchase_order.supplier_id %>" placeholder="Zenventory supplier ID" style="width: 100%;">
      </div>
      <div style="margin-bottom: 1lh;">
        <label style="display: block; color: var(--foreground2); margin-bottom: 0.25lh;">Required by date</label>
        <%= form.date_field :required_by_date, style: "width: 100%;" %>
      </div>
      <div style="margin-bottom: 1lh;">
        <label style="display: block; color: var(--foreground2); margin-bottom: 0.25lh;">Notes</label>
        <textarea name="warehouse_purchase_order[notes]" rows="3" placeholder="Add any notes for this purchase order..." style="width: 100%;"><%= purchase_order.notes %></textarea>
      </div>
    </div>
  </div>

  <div box-="round" style="margin-bottom: 1lh;">
    <strong>Line Items</strong>
    <div is-="separator"></div>
    <div style="margin-top: 0.5lh;">
      <%= render Components::Warehouse::LineItemsEditor.new(
        form: form,
        scope: :enabled,
        show_unit_cost: true,
        add_button_text: "Add Item"
      ) %>
    </div>
  </div>

  <row gap-="1" align-="center">
    <a href="<%= warehouse_purchase_orders_path %>">Cancel</a>
    <button type="submit" variant-="green">✓ <%= purchase_order.persisted? ? "Update Purchase Order" : "Create Purchase Order" %></button>
  </row>
<% end %>
