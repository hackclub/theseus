<% content_for :title, "New Template" %>

<div class="toolbar mb-0" style="border-bottom: none">
  <div class="flex-row">
    <a href="<%= warehouse_templates_path %>" class="text-muted" style="text-decoration: none">← Templates</a>
    <strong style="font-size: 1.15em;">New Template</strong>
  </div>
</div>

<div class="show-layout">
  <div class="show-main">
    <%= render "form", warehouse_template: @warehouse_template %>
  </div>
  <div class="show-sidebar">
    <section>
      <strong>Help</strong>
      <hr>
      <div class="mt-half text-muted">
        Templates define a reusable set of line items for warehouse orders. Shared templates are visible to all users.
      </div>
    </section>
  </div>
</div>
